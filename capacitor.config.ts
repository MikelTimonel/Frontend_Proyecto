import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.belcorp.belbeau.com',
  appName: 'belbeau',
  webDir: "dist/belbeau/browser"
};

export default config;
